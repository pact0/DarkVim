#!/bin/sh

export DARKVIM_RUNTIME_DIR="${DARKVIM_RUNTIME_DIR:-RUNTIME_DIR_VAR}"
export DARKVIM_CONFIG_DIR="${DARKVIM_CONFIG_DIR:-CONFIG_DIR_VAR}"
export DARKVIM_CACHE_DIR="${DARKVIM_CACHE_DIR:-CACHE_DIR_VAR}"

exec nvim -u "$DARKVIM_RUNTIME_DIR/dark/init.lua" "$@"
